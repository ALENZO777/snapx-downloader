## Packages
framer-motion | Page animations and list transitions
react-icons | Used for platform icons (Youtube, TikTok, etc) if Lucide is insufficient, but Lucide is standard. Sticking to Lucide for now.
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The backend provides endpoints for resolving video URLs and creating batch ZIP downloads.
We need to handle the ZIP download blob response specifically.
The app design should be modern, dark-themed, and focus on the input and results list.
